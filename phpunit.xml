<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.3/phpunit.xsd" bootstrap="vendor/autoload.php" colors="true">
  <testsuites>
    <testsuite name="Unit">
      <directory suffix="Test.php">./tests/Unit</directory>
    </testsuite>
    <testsuite name="Feature">
      <directory suffix="Test.php">./tests/Feature</directory>
    </testsuite>
    <testsuite name="v4">
      <directory suffix="Test.php">./tests/v4</directory>
    </testsuite>
  </testsuites>
  <coverage/>
  <php>
    <!-- PHP's 128M CLI default is exhausted while Pest is still loading the suite,
         so an unqualified `php artisan test` dies before running a single test.
         A full single-process suite (Unit + Feature + v4, ~3600 tests) creeps
         roughly 1.1-1.3 GB across per-test application reboots; 2G keeps the
         whole suite runnable locally while individual CI lanes stay far below it.
         Note: this <ini> entry also overrides any `-d memory_limit=` CLI flag. -->
    <ini name="memory_limit" value="2G"/>
    <env name="APP_ENV" value="testing" force="true"/>
    <env name="BCRYPT_ROUNDS" value="4"/>
    <env name="CACHE_DRIVER" value="array" force="true"/>
    <env name="CACHE_STORE" value="array" force="false"/>
    <env name="DB_CONNECTION" value="testing" force="false"/>

    <env name="MAIL_MAILER" value="array" force="true"/>
    <env name="QUEUE_CONNECTION" value="sync" force="true"/>
    <env name="SESSION_DRIVER" value="array" force="true"/>
    <env name="TELESCOPE_ENABLED" value="false"/>
    <env name="NIGHTWATCH_ENABLED" value="false"/>
  </php>
  <source>
    <include>
      <directory suffix=".php">./app</directory>
    </include>
  </source>
</phpunit>
