#!/bin/sh

set -eu

case "${1:-}" in
    -v)
        printf '%s\n' 'HAProxy version 2.8.26'
        ;;
    -vv)
        printf '%s\n' 'HAProxy version 2.8.26' 'Feature list : +SYSTEMD'
        ;;
    *)
        exit 64
        ;;
esac
