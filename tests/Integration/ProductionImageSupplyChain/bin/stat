#!/bin/sh

set -eu

if [ "${1:-}" = -c ]; then
    case "${3:-}" in
        */bin|*/bin/haproxy) printf '%s\n' 755:0:0 ;;
        *) printf '%s\n' 600:0:0 ;;
    esac

    exit 0
fi

exec /usr/bin/stat "$@"
