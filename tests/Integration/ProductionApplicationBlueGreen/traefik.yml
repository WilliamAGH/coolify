entryPoints:
  http:
    address: :80

providers:
  docker:
    endpoint: unix:///var/run/docker.sock
    exposedByDefault: false
    constraints: Label(`coolify.blueGreen.managed`, `true`)
  file:
    directory: /dynamic
    watch: true

accessLog:
  format: json

log:
  level: INFO
